---
- name: Learning fact variables
  hosts: all
  tasks:
    - name: Print OS names
      debug:
        var: ansible_distribution

    - name: Print Memory details
      debug:
        var: ansible_memory_mb
